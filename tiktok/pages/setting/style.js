import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    backgroundColor: "#fff",
    height: "100%",
    width: "100%",
  },
  header: {},
  body: {
    width: "100%",
    // height: "100%",
    backgroundColor: "#fff",
  },
});

export default styles;
